export type MemberPTPackage = {
  id: string
  memberId: string
  sessionsTotal: number
  sessionsRemaining: number
  purchaseDate: string
  expiresAt: string
  createdAt: string
  updatedAt: string
}
